# Models

The application trains Isolation Forest at startup from the synthetic invoice history, so no opaque binary model artifact is required in source control. `evaluation.json` stores reproducible synthetic scenario metrics generated by `src/evaluate.py`.
